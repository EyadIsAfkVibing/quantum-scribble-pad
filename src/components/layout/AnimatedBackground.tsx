import { useApp } from '@/contexts/AppContext';

export function AnimatedBackground() {
  // Aurora is now the primary background - this component is deprecated
  // Kept for compatibility but returns null
  return null;
}
